name: Test Actions

on:
  workflow_dispatch:
  push:
    paths:
      - "trigger-workflow.md"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Actions is working
        run: echo "✅ GitHub Actions is working! Snake workflow should be visible now."

      - name: List workflow files
        run: |
          echo "📁 Checking workflow files:"
          ls -la .github/workflows/ || echo "No .github/workflows directory found"
